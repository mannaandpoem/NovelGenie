INTENT_ANALYZER_PROMPT = """
# 根据以下用户输入，分析并提取：
1. 一个详细且扩展的网文描述（约 100 字）。
2. 该网文的最合适的类型。如果类型不明确，默认为 "都市"。
3. 网文的总目标字数。如果未指定，默认为 1000 字。
4. 网文的总卷数。如果未指定，默认为 1 卷。

# 用户输入
{user_input}

# 输出格式
请严格按照以下 JSON 格式输出：
```json
{{
  "description": "[扩展后的网文描述]",
  "genre": "[网文类型]",
  "word_count": [目标字数],
  "volume_count": [卷数]
}}
```
"""
